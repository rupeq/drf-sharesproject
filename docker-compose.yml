version: "3.7"

services:
  db:
    image: postgres
    container_name: postgres_db
    env_file:
      - db_keys.txt
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db

